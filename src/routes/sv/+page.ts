export const prerender = true;

// The Swedish home lives at /sv/ (the English home is /), so this one page keeps a
// trailing slash while every other page uses the default.
export const trailingSlash = 'always';
