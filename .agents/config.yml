# The repository's commands and paths — the single home for both.
#
# AGENTS.md, the skills and the convention checker read these values by key and never
# restate them. Changing a command means changing it here, in this repository's own copy:
# that is the override mechanism, and it is why no repository needs to fork the template.
#
# Fill every slot once, at adoption (README.md, step 3). After that this file holds real
# values and nothing else: a search of the copied tree for two opening braces must print
# nothing.
convention_version: "1"      # integer revision of the convention this copy came from
adopted_from: "fdd819d751034bc7872dd25fbe29bbee161fc8a2"                  # commit in aivara-se/.github this copy came from
repo: "aivara-se/aivara.se"
language: "Svelte"                     # e.g. Svelte, Go, HTML, or none
package_manager: "bun"       # bun, npm, cargo, or none — never a second one

# commands: what to run, as single-line strings with no double quote in them.
# `check` is the one wrapper command that humans, agents and CI all invoke; a step this
# repository does not have is the literal null, never a guess and never an empty string.
commands:
  check: "bun run check"
  test: "bun test"
  lint: "bun run format:check"
  build: "bun run build"
ci_workflow: ".github/workflows/checks.yml"               # path, or null when this repository has no CI

# paths: the documents that are authoritative for each subject. One fact has one home.
# Use `none` where this repository has no such document.
paths:
  architecture: "none"
  product: "PRODUCT.md"
  design: "DESIGN.md"
